#include "main.h"
#include <stdio.h>

/**
 * main - entry to function
 * Return: 1 if uppercase and 0 otherwise
 */

void print_triangle(void)
{
	int size = 3;
	int i = 0, j;

	while (i != size)
	{
		while (i != size)		
		{
			_putchar('#');
		}
	}
}