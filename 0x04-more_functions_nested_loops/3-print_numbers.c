#include "main.h"

/**
 * print_numbers - tests function
 * Return: 1 if uppercase and 0 otherwise
 */

void print_numbers(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		_putchar(i);
	}
	_putchar(10);
}
