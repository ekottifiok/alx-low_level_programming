#include "main.h"

/**
 * print_square - tests function
 * @size: value inputted
 * Return: 1 if uppercase and 0 otherwise
 */

void print_square(int size)
{
	int i = 0;

	while (i != size)
	{
		while (i != size)
		{
			_putchar('#');
		}
		}
	_putchar(10);
}
